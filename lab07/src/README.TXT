Задача 1а: 100%
a) Да се напише class Point, който реализира (имплементира)
b) Да се напишат атрибути на класа Point x и y със съответните методи за достъп (set&get методи)
c) Да се напишат 3те вида конструктори (по подразбиране, за общо ползване и за копиране)
	- public Point();
	- public Point(int x, int y);
	- public Point(Point point);
d) Да се пренапише (overwrite) метода public String toString(); 
	- за класа Point (връща символен низ, съдържащ описание на точките от вида: (x, y)
e) Да се напишат методи,
	- които връщат разстоянието от текущата точка до зададена точка
		public double distanceTo(Point pointTarget);
	- които връщат разстоянието от зададена точка до текущата точка
		public double distanceFrom(Point pointTarget);
		!!! Това прави същото като distanceTo, затова го пропуснах.
	- които връщат разстоянието между две зададени точки
		public double distanceFrom(Point sourceTarget, Point targetTarget);
		!!! Това го прекръстих на "distanceBetween", тъй като методът не борави с 
		!!! вътрешното състояние на текущата инстанция. 
		!!! Поради тази същата причина, вероятно, би било по-добре изобщо
		!!! да не е част то този клас.